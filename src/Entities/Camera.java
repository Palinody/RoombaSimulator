package Entities;

import Math.Vector2D;

public class Camera extends Shapes {

    public Camera(Vector2D origin, double offset_angle, float[] xCoord, float[] yCoord) {
        super(origin, offset_angle, xCoord, yCoord);
    }
}
